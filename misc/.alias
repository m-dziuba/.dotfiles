alias init_tfnb="sudo docker run --runtime=nvidia --gpus all -it -p 8888:8888 -p 8080:8080 --name tf -v ~/Desktop/magisterka:/tf tensorflow/tensorflow:latest-gpu-jupyter bash -c \"source /etc/bash.bashrc && pip install gensim pandas optuna optuna-dashboard scikit-learn seaborn spacy && python -m spacy download pl_core_news_lg && jupyter notebook --notebook-dir=/tf --ip 0.0.0.0 --no-browser --allow-root --NotebookApp.token=''\""
alias tfnb="docker start tf"
alias neww="new-window zsh && "
alias tmux-sessionizer="~/.local/scripts/tmux-sessionizer"
alias vi="nvim"
alias vim="nvim"
